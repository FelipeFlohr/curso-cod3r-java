public class Main {

    public static void main(String[] args) {

        // No Java há 3 principais tipos de distinção de operadores: Unários, binários, ternário
        /* Exemplo de operadores
        Unário: a++ <- Unário pois há apenas um operando
        Binário: 3 + 2 <- Binário pois há apenas dois operandos
        Ternário: v ? v : v <- Ternário pois há três operandos e dois operadores
         */

        // Prefix, postfix & infix
        // Prefix: ++a (operador e operando)
        // Postfix: a++ (operando e operador)
        // Infix: 3 + 2 (operador, operando e operador)
    }

}
